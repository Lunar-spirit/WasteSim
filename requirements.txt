fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
geoalchemy2==0.15.2
shapely==2.0.6
# pyogrio (not Fiona): CLAUDE.md section 4 lists both, but pyogrio alone
# reads GeoJSON/KML/Shapefile through the same GDAL drivers Fiona uses and
# is what geopandas.read_file(..., engine="pyogrio") calls — no need for a
# second GDAL binding doing the same job.
pyogrio==0.10.0
pyproj==3.6.1
geopandas==1.0.1
alembic==1.13.2
python-jose[cryptography]==3.3.0
# bcrypt pinned below 4.1: passlib 1.7.4's one-time backend self-test calls
# bcrypt with its own 512-byte probe string, and bcrypt>=4.1 raises instead
# of truncating past 72 bytes there, crashing every single password hash.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
slowapi==0.1.9
structlog==24.4.0
httpx==0.27.2
celery==5.4.0
redis==5.0.8
# Official MinIO client (not boto3): the stack only needs put/get/stat on one
# bucket, and the MinIO SDK's API matches the design's "object key" language
# 1:1. CLAUDE.md section 4 says "MinIO / S3" without naming a client library,
# so this is the one library choice this session made on its own.
minio==7.2.9
pandas==2.2.3
openpyxl==3.1.5
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
factory-boy==3.3.1
ruff==0.6.9
black==24.8.0
mypy==1.11.2
